+!start.

+!start
 <- .print("Robô iniciado!");
    !explorar.

+!explorar
 <- .print("Explorando o ambiente...");
    !decidir.

+!decidir
 <- .print("Tomando decisão...");
    !agir.

+!agir
 <- .print("Executando ação!");
